{{- if or (eq .chezmoi.hostname "cachyos-x8664") (eq .chezmoi.hostname "macbook-air-m4") }}
  .config/fish/
  .config/nvim/
  .config/polybar/
  .config/ranger/
  .config/starship.toml
  .gitconfig
  .tmux.conf
{{- end }}

{{- if eq .chezmoi.hostname "cachyos-x8664" }}
  .config/aerospace/
  .config/borders/
  .config/bottom/
  .config/sketchybar/
  .config/raycast/
{{- end }}

{{- if eq .chezmoi.hostname "macbook-air-m4" }}
  .config/niri/
  .local/share/easyeffects/
  .local/bin/git
  .local/src
  .chezmoiscripts/build-git-wrapper.sh
{{- end }}

{{- if hasPrefix .chezmoi.hostname "proxmox-" }}
  # TODO: 必要に応じて個別ファイルを許可
  *
{{- end }}

mise.toml
AGENTS.md
GEMINI.md
README.md
apm.lock.yaml
apm.yml*
apm_modules/
skills
.zshrc
